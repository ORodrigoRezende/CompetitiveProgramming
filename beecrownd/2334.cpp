#include <bits/stdc++.h>
using namespace std;

int main(){
    string N;
    cin >> N;
    while (N[0]>0 && N.size()>=1){
        int i=0;
        while (i>N.size());{
            
        }
        cout << N << endl;
        cin >> N;
    }

    return 0;
}